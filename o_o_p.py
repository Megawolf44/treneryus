# 1. Что такое класс(самое базовое)
"""
Docstring для oop
Класс - это чертеж или шаблон для создания объектов.
         Аналогия:
Класс - рецепт пирога
Объект = конкретный испечённый пирог по этому рецепту
"""
'''
class Pie: # ← это класс (чертеж) Пирог
    def __init__(self, filling): # конструктор пfiling(подача)

        self.filling = filling # отрибут объекта

        self.ready = False # ready(готовый)

    def bake(self):   # Метод bake(печь)
        self.ready = True
        print(f"Пирог с {self.filling} испечен!")
# Создаем объекты (пироги)
applesauce = Pie("яблоко")  # объект 1
cherry = Pie("вишня")  # объект 2

applesauce.bake()
print(cherry.ready)
cherry.bake()
"""
2. Основные элементы класса (все до одной мелочи)

Элемент                         что это                              обязателен?            Пример
▬▬▬▬▬▬▬▬                       ▬▬▬▬▬▬▬▬▬                            ▬▬▬▬▬▬▬▬▬▬▬            ▬▬▬▬▬▬▬
class Имя:                  объявление класса                           Да               class Машина:

init(self,...)            конструктор - вызывается при              почти всегда      def init(self,цвет,год):
                          создании объекта

self                     ссылка на текущий объект                      Да             self.цвет = цвет

атрибуты                 переменные, уникальные для                     ▬             self.цвет, self.скорость
экземпляра               каждого объекта

методы                   функции внутри класса                         ▬             def ехать(self):...

атрибуты класса          общие бля всех объектов класса                 нет           калёса = 4

@classmethod            метод класса(работает с классом)                нет          @classmethod def из цвета(cls, c);

@staticmethod          Обычная функция внутри класса                    нет          @saticmethod def сигнал():...
"""

# ▬▬▬ 3. Наследование (подклассы) - самое главное
"""
Подкласс(дочерний класс)наследует все от родительского класса и может:
• использовать его атрибуты и методы
• переопределять (переписывать )их 
• доюовляь свои

▬▬▬▬▬▬ АНАЛОГИЯ ▬☻▬▬☻
Родитель = "Транспортное средство"
Подкласс = "Автомобиль" 
"""

class Transport:  # родительский класс (базовый)
    def __init__(self, speed):
        self.speed = speed
        self.engine_running = False # engine(двигатель) running(работает) 

    def started(self): # started(начатый)
        self.engine_running = True
        print('Двигатель заведён')

    def drive(self):
        if self.engine_running:
            print(f'Едим со скоростью {self.speed} км/ч')
        else:
            print("Сначала заведи двигатель")
class Car(Transport): # подкласс наследует от Транспорт
    def __init__(self, speed, collor, model):
        super().__init__(speed)  # Вызываем конструктор родителя
        self.collor = collor
        self.model = model
        self.door_open = False

        # Переопределяем (override)метод родителя
    def drive(self):
        if self.engine_running:
            print(f"{self.model} {self.collor} цвета едет со скоростью {self.speed} км/ч")

        else:

           print("Завиди двигатель, лентяй! ")
            
    def open_the_dor(self):
        self.engine_running = True
        print("Двери открыты")
        
# Использование

lada = Car(180, "Синий", "Lada Vesta")
lada.started()  # от родителя
lada.drive()    # переопределенный метод
lada.open_the_dor()   # свой метод

# 4. Ключевые слова и конструкции наследования(все в одной)


Конструкция                     Что делает                                           Когда используется
▬▬▬▬▬▬▬▬▬▬▬                     ▬▬▬▬▬▬▬▬▬▬▬                                          ▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬
class                      Дочерний наследует всё от Родитель                     всегда при наследовании
Дочерний(Родитель) 

super().init(...)          вызывает конструктор родителя                         почти всегда в init дочернего

super().метод()           вызывает метод родителя(если переопределили)          когда нужно дополнить, а не заменить

isinstance(obj, Класс)    проверяет,является ли obj экземпляром                 isinstance(lada,Транспорт)→ True
                               Класса или его потомком

issubclass(Доч, Род)      проверяет, является ли Доч подклассом Род             issubclass(Автомобиль,Транспорт)→ True


# 5. Множественное наследование (очень важное понимание)


class flying: # летающий
    def fly(self): # лететь
        print('Лечу!')
class floating:    # Плавающий
    def to_sail(self): # Плыть
        print('Плыву')     

class amphibian(flying, floating): # Амфибия наследует от двух родителей
    pass
    
amfibia = amphibian()
amfibia.fly()     # Лечу
amfibia.to_sail() # Плыву

# ▬▬▬▬▬▬▬☻ Порядок наследования важен (MRO-Method Resolution Order)

print(amphibian.mro()) # [<class '__main__.amphibian'>, <class '__main__.flying'>, <class '__main__.floating'>, <class 'object'>]


# 6. Полиморфизм(один из столпов ООП)

# Один и тот же метод у разных классов делает разные вещи

def make_them_go(vehcle): # Заставить ехать
    vehcle.drive()   #один вызов - разные реализации

lada = Car(180, "синий", "Lada")
truck = Transport(90)

lada.started()  # Заводим двигатель
truck.started()

make_them_go(lada)     # Lada синий цвет едет...
make_them_go(truck)    # Едем со скоростью 90км/ч



# 7. Инкапсуляция (защита данных)
# В Python нет строгой приватности, но есть договоренности

class bank:
    def __init__(self):
        self.balanc = 1000   # Публичный
        self._pin = 1234    # защищенный (по договоренности)
        self.__sikret = 'пороль'  # приватный (имя меняется на _Банк__секрет)
    def get_a_secret(self):
        return self.__sikret 

# 8. Абстрактные классы(если хочешь запретить создание экземпляров)

from abc import ABC, abstractclassmethod
class figure(ABC):
    @abstractclassmethod
    def square(self):
        pass
class a_circle(figure):
    def __init__(self, r):
        
        self.r = r
    def square(self):
        return 3.14 * self.r ** 2



class Car:
    def __init__(self, collor, speed, model):
        self.collor = collor
        self.speed = speed
        self.madel = model
    
    def drive(self):
        print(f"{self.madel} {self.collor} цвета едит со скоростью {self.speed} км/ч")
        # Теперь создаем машину 

ferrari = Car("Красная", 300, "Фиррари")
bmw = Car("Синяя", 250, "BMW")

ferrari.drive()
bmw.drive()

Что мы получили:

• Один раз написали шаблон (класс Машинка)
• Много раз создали машинки (объекты)
• Каждая машинка знает свой цвет, скорость, марку
• Каждая умеет ехать (метод поехать)

Самые важные 5 предложений, которые нужно запомнить

• Класс — это рецепт / чертёж / шаблон.
• Объект — это конкретная вещь, сделанная по этому чертежу.
• self — это слово, которое значит «я сам» (эта конкретная машинка).
• __init__ — это то, что происходит, когда мы создаём новую машинку (задаём ей свойства).
• Метод — это действие, которое умеет делать каждая машинка (поехать, затормозить, посигналить).


# Самый маленький код, который ты можешь запустить прямо сейчас

class Cat:
    def __init__(self, name, collor, years, sleep):
        self.name = name 
        self.collor = collor
        self.years = years
        self.sleep = sleep
    def meow(self):
        print(f'{self.name} ({self.collor})ему {self.years} лет говорит: Мяу-Мяу! {self.sleep}' )

myrzik = Cat('Мурзик', 'серый', 5, 'любит спать')
barsik = Cat('Барсик', 'рыжий', 4 , 'ложится спать') 

myrzik.meow()
barsik.meow()


class Car:
    def __init__(self, name, color): # функция конструктор, в которой описываются атрибуты класса
        self.name = name # self обозначает принадлежность этой переменной только к этому классу
        self.color = color


    def print_info(self):
        print(f"{self.name} is {self.color}")


niva = Car("Хантер", "Мокрый асфальт") # создание экземпляра класса
lamba = Car("Авентодор", "Оранжевый")        
    
niva.print_info() # применение метода к экземплчру класса
lamba.print_info()
print(niva.name)
print(lamba.color)


class Dog:
    def __init__(self, name, age):
        self.name = name
        self.age = age

    def description(self):
        return f"{self.name} is {self.age} years old"

    def speak(self, sound):
        return print(f"{self.name} говорит {sound}")

spik = Dog('Marley', 10)

spik.speak('Гаф')

'''
'''
# Повторение основных ОПРЕДЕЛЕНИЙ ООП

class Car: # определение класса
    def __init__(self,brend, model, color, age): # конструктор класса
        self.brend = brend # атрибуты класса
        self.model = model # атрибуты класса
        self.color = color # атрибуты класса
        self.age = age # атрибуты класса

    def start_engine(self): # метод класса
        print(f"Запустить двигатель") # метод класса

    def drive(self, distance): # метод класса
        print(f"Вождение {distance} km") # метод класса    

car1 = Car("Niva", "Hunter", "Grey", 2010) # создание экземпляра класса
car2 = Car("BMW", "X5", "Black", 2015) # создание экземпляра класса  
car3 = Car("Tesla", "Model S", "White", 2020) # создание экземпляра класса

print(car1.model, car1.color) # доступ к атрибуту экземпляра класса
print(car2.color) # доступ к атрибуту экземпляра класса

car1.start_engine() # вызов метода класса


car1.drive(100) # вызов метода класса
car2.drive(200) # вызов метода класса
car3.drive(300) # вызов метода класса

print(car1) # вывод экземпляра класса
'''
'''
                        ▬▬▬▬▬▬ ☻ Ключевые особенности ООП: ☻ ▬▬▬▬▬▬
• Абстракция - это принцип ООП,который позволяет скрыть сложные детали реализации и предоставить простой
интерфейс для взаимодействия с объектами.Это помогает разработчикам сосредоточиться на том, что объект делает, 
а не на том, как он это делает. Абстракция достигается через использование классов и методов, 
которые скрывают внутреннюю логику и предоставляют только необходимые функции для взаимодействия с объектом.

• Инкапсуляция - это принцип ООП, который позволяет объединить данные и методы, которые работают с этими данными,
внутри одного класса. Это помогает защитить данные от несанкционированного доступа и обеспечивает контроль над тем,
 как данные изменяются. Инкапсуляция достигается через использование модификаторов доступа (например, public, private) 
 и методов доступа (геттеры и сеттеры), которые позволяют контролировать доступ к данным и обеспечивают безопасность данных.

• Наследование - это принцип ООП, который позволяет создавать новый класс на основе существующего класса.
 Новый класс (называемый подклассом или дочерним классом) наследует атрибуты и методы существующего класса 
 (называемого суперклассом или родительским классом), и может добавлять свои собственные атрибуты и методы или 
 переопределять существующие. Наследование позволяет повторно использовать код и создавать иерархии классов,
   что способствует организации и структуре программного обеспечения.

• Полиморфизм - это принцип ООП, который позволяет объектам разных классов обрабатывать данные по-разному, 
даже если они имеют одинаковый интерфейс. Это означает, что объекты разных классов могут быть использованы взаимозаменяемо,
и каждый объект будет реагировать на вызов метода по-своему. Полиморфизм достигается через наследование и
переопределение методов, а также через использование интерфейсов и абстрактных классов.
Это позволяет создавать гибкие и расширяемые программы, которые могут работать с различными 
типами объектов без необходимости знать их конкретные классы.      
'''
'''
class ElectricCar(Car): # подкласс наследует от Car
    def __init__(self, brend, model, color, age, battery_capacity): # конструктор подкласса 
        super().__init__(brend, model, color, age) # вызов конструктора родителя
        self.battery_capacity = battery_capacity # новый атрибут для электрокара

    def charge(self): # новый метод для электрокара
        print("Зарядка аккумулятора") # новый метод для электрокара

    def drive(self, distance): # переопределение метода drive для электрокара
        super().drive(distance) # вызов метода drive родителя
        self.battery_capacity -= distance * 0.1 # уменьшение заряда батареи при езде
        print(f"Ёмкость аккумулятора теперь равна: {self.battery_capacity} kWh") # вывод текущей емкости батареи

electric_car = ElectricCar("Tesla", "Model 3", "Red", 2021, 75) # создание экземпляра подкласса

print(electric_car.brend, electric_car.model, electric_car.color, electric_car.age, electric_car.battery_capacity) # доступ к атрибутам подкласса
electric_car.start_engine() # вызов метода родителя
electric_car.drive(50) # вызов переопределенного метода подкласса
electric_car.charge() # вызов нового метода подкласса

car = Car("Toyota", "Corolla", "Blue", 2018) # создание экземпляра класса Car

start_car = (car) # присвоение экземпляра класса Car переменной start_car
# start_car(electric_car) # попытка вызвать экземпляр класса Car как функцию, что вызовет ошибку TypeError, так как экземпляры классов не являются вызываемыми объектами

# Тренируемся создовать классы и методы, создовать подклассы, наследовать методы и атрибуты, переопределять методы, создавать новые методы и атрибуты в подклассах, а также использовать super() для вызова методов родителя.

class enimal: # определение класса
    def __init__(self, name, age): # конструктор класса
        self.name = name # атрибуты класса
        self.age = age # атрибуты класса

    def eat(self): # метод класса
        print("Животное ест") # метод класса
        
    def sleep(self): # метод класса
        print("Животное спит") # метод класса

class Cat(enimal): # подкласс наследует от класса enimal
    def __init__(self, name, age, color): # конструктор подкласса
        super().__init__(name, age) # вызов конструктора родителя
        self.color = color # новый атрибут для подкласса

    def purr(self): # новый метод для подкласса
        print("Кошка мурлычет") # новый метод для подкласса
           
class Dog(enimal): # подкласс наследует от класса enimal
    def __init__(self, name, age, breed): # конструктор подкласса
        super().__init__(name, age)
        self.breed = breed # новый атрибут для подкласса

    def bark(self): # новый метод для подкласса
        print("Собака лает") # новый метод для подкласса

class Bird(enimal): # подкласс наследует от класса enimal
    def __init__(self, name, age, wingspan): # конструктор подкласса
        super().__init__(name, age)
        self.wingspan = wingspan # новый атрибут для подкласса
    
    def fly(self): # новый метод для подкласса
        print("Птица летит") # новый метод для подкласса
    
cat = Cat("Мурзик", 5, "Серый") # создание экземпляра подкласса Cat
dog = Dog("Барсик", 3, "Лабрадор") # создание экземпляра подкласса Dog
bird = Bird("Кеша", 2, 0.5) # создание экземпляра подкласса Bird   

cat.eat() # вызов метода родителя
cat.sleep() # вызов метода родителя 
cat.purr() # вызов метода подкласса
dog.eat() # вызов метода родителя
dog.sleep() # вызов метода родителя
dog.bark() # вызов метода подкласса
bird.eat() # вызов метода родителя
bird.sleep() # вызов метода родителя
bird.fly() # вызов метода подкласса

class arel(Bird): # подкласс наследует от класса Bird
    def __init__(self, name, age, wingspan, can_fly): # конструктор подкласса
        super().__init__(name, age, wingspan) # вызов конструктора родителя
        self.can_fly = can_fly # новый атрибут для подкласса

    def fly(self): # переопределение метода fly для подкласса
        if self.can_fly:
            print("Птица летит") # если может летать
        else:
            print("Птица не может летать") # если не может летать
    

# Самые важные 5 предложений, которые нужно запомнить
'''
'''
• Класс — это рецепт / чертёж / шаблон.
• Объект — это конкретная вещь, сделанная по этому чертежу
• self — это слово, которое значит «я сам» (эта конкретная машинка).
• __init__ — это то, что происходит, когда мы создаём новую машинку (задаём ей свойства).
• Метод — это действие, которое умеет делать каждая машинка (поехать, затормозить, посигналить).    


'''
class Cat:
    def __init__(self, name, color, age):# конструктор класса, который задает атрибуты класса
        self.name = name # атрибуты класса
        self.color = color # атрибуты класса
        self.age = age # атрибуты класса
        
        
    def is_sleeping(self):
        return self.sleep

    def meow(self): # метод класса
        if self.sleep:
            return print(f'{self.name} ({self.color}), {self.age} лет мурлычет: мррр...')
        elif self.age < 2: # если котенок, то мяукает тихо
            return print(f'{self.name} ({self.color}), {self.age} лет: мяу...')  # если котенок, то мяукает тихо
        else:
            return print(f'{self.name} ({self.color}), {self.age} лет: МЯУ-МЯУ!')
    
    def sleep(self): # метод спать
        self.sleep = True
        print(f'{self.name} ложится спать. Хррррр...') 
        
        if self.sleep:

            return print(f'{self.name} ({self.color}), {self.age} лет мурлычет: мррр...')
    
    def purr(self): # метод мурлыкать

        print(f'{self.name} мурлычет')
    


myrzik = Cat('Мурзик', 'серый', 3)
barsik = Cat('Барсик', 'рыжий', 1)


myrzik.meow()
barsik.meow()
myrzik.sleep() # Мурзик ложится спать. Хррррр...
myrzik.purr()





